spring:
  data:
    mongodb:
      username: glaymet
      password: ${MONGO_PASS}
      host: ${MONGO_HOST}
      port: ${MONGO_PORT}
      database: customer
      authentication-database: admin

server:
  port: 8090

springdoc:
  api-docs:
    path: /customers/v3/api-docs
